import { PostHeaderContentsProps } from '@/types/postHeader';
import React from 'react';

const PostHeaderContent: React.FC<PostHeaderContentsProps> = ({
  description,
  date,
  tags,
}) => {
  return <div>PostHeaderContent</div>;
};

export default PostHeaderContent;
